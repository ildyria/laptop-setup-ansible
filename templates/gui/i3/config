# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!


# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font pango:monospace 8
font pango: DejaVu Sans Mono for Powerline, Terminus Bold Semi-Condensed 8
# font pango:System San Franciso Display 12
#font pango:DejaVu Sans Mono 8

# exec --no-startup-id feh --bg-scale ~/.config/wallpaper.jpg
exec --no-startup-id compton

## Size of border ##
new_window pixel 2

set $win Mod4
set $alt Mod1

# Use Mouse+$alt to drag floating windows to their wanted position
floating_modifier $alt

## Navigation ##
set $left j
set $down k
set $up l
set $right semicolon

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1  "1:1"
set $ws2  "2:2"
set $ws3  "3:3"
set $ws4  "4:4"
set $ws5  "5:5"
set $ws6  "6:6"
set $ws7  "7:7"
set $ws8  "8:8"
set $ws9  "9:9"
set $ws0  "10:🔐"
set $wsa  "11:-"
set $wsb  "12:="

# we need to start on the right space to prevent the [1] from showing.
exec --no-startup-id "i3-msg 'workspace 1:1'"

# font pango:JetBrainsMono Nerd Font 10
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +2% 
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -2%
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +1%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 1%-
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle 

assign [class="Code"] $ws2

assign [class="Firefox"] $ws3
assign [class="Google-chrome"] $ws3

assign [class="thunderbird"] $ws4

assign [class="Emacs"] $ws5
assign [class="libreoffice"] $ws5

assign [class="Slack"] $ws6

assign [class="Evince"] $ws7

assign [class="Gimp"] $ws8
assign [class="vlc"] $ws8
assign [class="Siril"] $ws8

assign [class="Nautilus"] $ws9

assign [class="KeePass"] $ws0
assign [class="discord"] $ws0

assign [instance="skype"] $wsA
assign [class="Signal"] $wsA
assign [class="zoom"] $wsA

# exec --no-startup-id firefox
exec --no-startup-id google-chrome
exec --no-startup-id thunderbird

# start a terminal
bindsym $alt+Shift+Return exec i3-sensible-terminal

bindsym $alt+Shift+k exec keepass2 ; exec i3-msg 'workspace 10:🔐'
bindsym $win+e exec nautilus ; exec i3-msg 'workspace 9:9' # windows compatibility
bindsym $win+x exec i3lock-fancy-rapid 5 3
bindsym $win+p exec scrot '%Y-%m-%d_$wx$h_%H:%M:%S.png' -e 'mv $f ~/Pictures/Screenshots/'

# let's start a bunch of utilities (autolock after 10 minutes, nm-applet for the wifi)
exec --no-startup-id xautolock -time 3 -locker 'i3lock-fancy-rapid 5 3'

# kill focused window
bindsym $alt+F4 kill
# change focus
bindsym $win+$left focus left
bindsym $win+$down k focus down
bindsym $win+$up focus up
bindsym $win+$right focus right
# alternatively, you can use the cursor keys:
bindsym $win+Left focus left
bindsym $win+Down focus down
bindsym $win+Up focus up
bindsym $win+Right focus right
# move focused window
bindsym $win+Shift+$left move left
bindsym $win+Shift+$down move down
bindsym $win+Shift+$up move up
bindsym $win+Shift+$right move right
# alternatively, you can use the cursor keys:
bindsym $win+Shift+Left move left
bindsym $win+Shift+Down move down
bindsym $win+Shift+Up move up
bindsym $win+Shift+Right move right
# split in horizontal orientation
bindsym $win+h split h
# split in vertical orientation
bindsym $win+v split v
# enter fullscreen mode for the focused container
bindsym $win+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $win+Shift+s layout stacking
bindsym $win+Shift+t layout tabbed
bindsym $win+Shift+e layout toggle split

# toggle tiling / floating
bindsym $win+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $win+space focus mode_toggle

bindsym $win+Tab focus output left
# Move the current workspace to the next output
# (effectively toggles when you only have two outputs)
bindsym $win+Shift+Tab move workspace to output left

# focus the parent container
bindsym $win+a focus parent

# focus the child container
#bindsym $win+d focus child

##
## Workspaces
##

## Support NumPad with code-keys ##
#     [+] [-] |      [86] [82]
# [7] [8] [9] | [79] [80] [81]
# [4] [5] [6] | [83] [84] [85]
# [1] [2] [3] | [87] [88] [89]
#     [0]     |      [90]

## 0 ##
workspace $ws0 output PRIMARY_OUTPUT
bindsym $alt+0 workspace $ws0
bindsym $alt+Shift+0 move container to workspace $ws0
bindcode $alt+90 workspace $ws0
bindcode $alt+Shift+90 move container to workspace $ws0

## 1 ##
workspace $ws1 output PRIMARY_OUTPUT
bindsym $alt+1 workspace $ws1
bindsym $alt+Shift+1 move container to workspace $ws1
bindcode $alt+87 workspace $ws1
bindcode $alt+Shift+87 move container to workspace $ws1

## 2 ##
workspace $ws2  output PRIMARY_OUTPUT
bindsym $alt+2 workspace $ws2
bindsym $alt+Shift+2 move container to workspace $ws2
bindcode $alt+88 workspace $ws2
bindcode $alt+Shift+88 move container to workspace $ws2

##  3 ##
workspace $ws3 output PRIMARY_OUTPUT
bindsym $alt+3 workspace $ws3
bindsym $alt+Shift+3 move container to workspace $ws3
bindcode $alt+89 workspace $ws3
bindcode $alt+Shift+89 move container to workspace $ws3

## 4 ##
workspace $ws4 output PRIMARY_OUTPUT
bindsym $alt+4 workspace $ws4
bindsym $alt+Shift+4 move container to workspace $ws4
bindcode $alt+83 workspace $ws4
bindcode $alt+Shift+83 move container to workspace $ws4

## 5 ##
workspace $ws5  output PRIMARY_OUTPUT
bindsym $alt+5 workspace $ws5
bindsym $alt+Shift+5 move container to workspace $ws5
bindcode $alt+84 workspace $ws5
bindcode $alt+Shift+84 move container to workspace $ws5

## 6 ##
workspace $ws6 output PRIMARY_OUTPUT
bindsym $alt+6 workspace $ws6
bindsym $alt+Shift+6 move container to workspace $ws6
bindcode $alt+85 workspace $ws6
bindcode $alt+Shift+85 move container to workspace $ws6

## 7 ##
workspace $ws7 output PRIMARY_OUTPUT
bindsym $alt+7 workspace $ws7
bindsym $alt+Shift+7 move container to workspace $ws7
bindcode $alt+79 workspace $ws7
bindcode $alt+Shift+79 move container to workspace $ws7

## 8 ##
workspace $ws8 output PRIMARY_OUTPUT
bindsym $alt+8 workspace $ws8
bindsym $alt+Shift+8 move container to workspace $ws8
bindcode $alt+80 workspace $ws8
bindcode $alt+Shift+80 move container to workspace $ws8

## 9 ##
workspace $ws9 output PRIMARY_OUTPUT
bindsym $alt+9 workspace $ws9
bindsym $alt+Shift+9 move container to workspace $ws9
bindcode $alt+81 workspace $ws9
bindcode $alt+Shift+81 move container to workspace $ws9


## - ##
workspace $wsA output PRIMARY_OUTPUT
bindsym $alt+minus workspace $wsA
bindsym $alt+Shift+minus move container to workspace $wsA
bindcode $alt+82 workspace $wsA
bindcode $alt+Shift+82 move container to workspace $wsA


## + ##
workspace $wsB output PRIMARY_OUTPUT
bindsym $alt+equal workspace $wsB
bindsym $alt+Shift+equal move container to workspace $wsB
bindcode $alt+86 workspace $wsB
bindcode $alt+Shift+86 move container to workspace $wsB

## Navigation of workspaces (go to, move to) ##
# bindcode $alt+86 workspace next
bindsym $alt+Right workspace next
# bindcode $alt+Shift+86 move container to workspace next
bindsym $alt+Shift+Right move container to workspace next

# bindcode $alt+82 workspace prev
bindsym $alt+Left workspace prev
# bindcode $alt+Shift+82 move container to workspace prev
bindsym $alt+Shift+Left move container to workspace prev


# reload the configuration file
bindsym $win+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $alt+F2 exec --no-startup-id "~/.config/i3/rofi/bin/launcher"
bindsym $win+shift+v exec --no-startup-id pavucontrol
# bindsym $win+shift+q exec --no-startup-id loginctl terminate-user $USER
# bindsym control+$alt+Right workspace next
# bindsym control+$alt+Left workspace prev
bindsym $win+shift+q exec --no-startup-id ~/.config/i3/rofi/bin/powermenu
bindsym Print exec --no-startup-id maim -o -b 3 -s | convert - \( +clone -background black -shadow 80x3+5+5 \) +swap -background none -layers merge +repage ~/Pictures/$(date +%Y-%m-%d-%H-%M-%S).png
bindsym $win+Print exec --no-startup-id maim -o --window=root | convert - \( +clone -background black -shadow 80x3+5+5 \) +swap -background none -layers merge +repage ~/Pictures/$(date +%Y-%m-%d-%H-%M-%S).png
# bindsym shift+Print exec import ~/Pictures/$(date +%Y-%m-%d-%H:%M:%S).png
bindsym control+$alt+r exec --no-startup-id "~/.config/i3/bin/chwal.sh"
bindsym $win+m exec --no-startup-id ~/.config/i3/rofi/bin/music

##
## resize mode
##
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym $left resize shrink width 1 px or 1 ppt
        bindsym $down resize grow height 1 px or 1 ppt
        bindsym $up resize shrink height 1 px or 1 ppt
        bindsym $right resize grow width 1 px or 1 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 1 px or 1 ppt
        bindsym Down resize grow height 1 px or 1 ppt
        bindsym Up resize shrink height 1 px or 1 ppt
        bindsym Right resize grow width 1 px or 1 ppt

        # same bindings, but for the arrow keys
        bindsym Control+Left resize shrink width 5 px or 5 ppt
        bindsym Control+Down resize grow height 5 px or 5 ppt
        bindsym Control+Up resize shrink height 5 px or 5 ppt
        bindsym Control+Right resize grow width 5 px or 5 ppt

        ## Title / show / hide border ##
        bindsym o border toggle

        # back to normal: Enter or Escape or $win+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $win+r mode "default"
}

bindsym $win+r mode "resize"


# Set colors from Xresources
# Change 'color7' and 'color2' to whatever colors you want i3 to use 
# from the generated scheme.
# NOTE: The '#f0f0f0' in the lines below is the color i3 will use if
# it fails to get colors from Xresources.
set_from_resource $fg i3wm.color7 #f0f0f0
set_from_resource $bg i3wm.color2 #f0f0f0

# class                 border  backgr. text indicator child_border
# client.focused          $bg     $bg     $fg  $bg       $bg
# client.focused_inactive $bg     $bg     $fg  $bg       $bg
# client.unfocused        $bg     $bg     $fg  $bg       $bg
# client.urgent           $bg     $bg     $fg  $bg       $bg
# client.placeholder      $bg     $bg     $fg  $bg       $bg

# client.background       $bg

client.focused            #1060A0 #1060A0 #111111 #1060A0
client.focused_inactive   #1c1c1c #1c1c1c #5f676a
client.unfocused          #1c1c1c #1c1c1c #888888 #222222
client.urgent             #2f343a #009000 #ffffff #009000

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $alt+Shift+r restart
# Shutdown, Reboot, Lock Screen, and Logout
# 1060A0
set $sysmenu "system:   [R]eload i3    [P]oweroff    re[B]oot    [L]ock    logou[T]"
# bindsym $win+Shift+Return      mode $sysmenu
bindsym $alt+Shift+F4          mode $sysmenu
mode $sysmenu {
    bindsym p         exec /sbin/shutdown -P now
    bindsym b         exec /sbin/shutdown -r now
    bindsym l         exec i3lock -c 000000, mode "default"
    bindsym r         restart, mode "default"
    bindsym t         exec --no-startup-id i3-msg exit, mode "default"
    bindsym Return    mode "default"
    bindsym Escape    mode "default"
}


# gaps
# gaps outer|horizontal|vertical|top|left|bottom|right <gap_size>[px]
# gaps outer 8
# gaps inner 8

# workspace 1 gaps inner 30
# workspace 1 gaps outer 30
# workspace 2 gaps inner 50
# workspace 2 gaps outer 50

# RULEs 
# default_floating_border pixel 4
# default_border pixel 0
# for_window [class=".*"] border pixel 4
for_window [class="Pavucontrol"] floating enable
for_window [class="Polybar"] floating enable
for_window [class="polybar"] floating enable

# Startup Programs 

exec --no-startup-id /usr/lib/gnome-settings-daemon/gsd-xsettings
exec_always --no-startup-id gnome-power-manager

# exec --no-startup-id picom --config ~/.config/i3/picom/picom.conf --experimental-backends &
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &
exec --no-startup-id "$HOME/.config/i3/bin/chwal.sh"

# exec_always --no-startup-id dunst -conf $HOME/.config/i3/dunst/dunstrc &
#exec --no-startup-id redshift-gtk &
exec --no-startup-id $HOME/.config/i3/polybar/launch.sh &
exec --no-startup-id libinput-gestures-setup start &

